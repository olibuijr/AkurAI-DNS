name = "akurai-dns"
repo = "olibuijr/AkurAI-DNS"
build = "cargo build --release --target x86_64-unknown-linux-musl"
artifact = "target/x86_64-unknown-linux-musl/release/akurai-dns"
version_file = "VERSION"
cargo_version = "package"
gate_fmt = true
gate_clippy = true
gate_test = true
