# AGENTS.md

Documentation for **AkurAI-DNS** lives in the **akurai-notes** MCP — the single
source of truth for AkurAI docs. It is not duplicated in this repo.

- **Canonical note:** `AkurAI-DNS — Docs`
- **Index:** `AkurAI EC2 — Documentation Index`
- Retrieve via the `akurai-notes` MCP: `search_notes "AkurAI-DNS — Docs"`, then `get_note <id>`.

Secrets live in the **akurai-passvault** MCP, never in this repo (this project has none).

> `CLAUDE.md` intentionally contains only `@AGENTS.md`.
